version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - sqlite_data:/data
    environment:
      - NODE_ENV=development
      - DB_FILE=/data/data.sqlite

volumes:
  sqlite_data:
